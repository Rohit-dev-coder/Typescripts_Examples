function calculate(a, b, c) {
    let sum = a + b + c;

    console.log(sum);
}

let arr = [3, 4, 5];

calculate(arr); // js convert to string and pass as a single argument, so it will print 3,4,5 instead of sum of 3, 4 and 5