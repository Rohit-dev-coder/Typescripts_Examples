function calculate(a, b, c) {
    let sum = a + (b + c);

    console.log(sum);
}

let arr = [3, 4, 5];

calculate(arr); // first argument is the array, second and third are undefined, so we get NaN as the result.